import PremiumCalculator from "@/components/surveyor/PremiumCalculator";

export const metadata = {
  title: "Premium Calculator | Surveyor Portal",
  description: "Calculate builder liability policy premiums based on risk factors.",
};

export default function PremiumCalculatorPage() {
  return <PremiumCalculator />;
}
